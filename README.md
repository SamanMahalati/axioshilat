# فروشگاه آنلاین شیلات و آبزیان - دمو وب‌سایت

یک وب‌سایت فروشگاهی تک‌صفحه‌ای حرفه‌ای و مدرن برای فروش محصولات شیلات، آبزیان، ماهیگیری و غذاهای دریایی.

## ویژگی‌ها

- ✨ طراحی مدرن و حرفه‌ای
- 📱 کاملاً ریسپانسیو (موبایل، تبلت، دسکتاپ)
- 🎨 رابط کاربری زیبا با Tailwind CSS
- ⚡ انیمیشن‌های روان با Framer Motion
- 🌐 پشتیبانی کامل از فارسی و RTL
- 🚀 ساخته شده با Next.js 14
- 🎯 بهینه‌سازی شده برای SEO

## نصب و راه‌اندازی

### پیش‌نیازها

- Node.js 18 یا بالاتر
- npm یا yarn

### مراحل نصب

1. نصب وابستگی‌ها:

```bash
npm install
```

2. اجرای پروژه در حالت توسعه:

```bash
npm run dev
```

3. باز کردن مرورگر و رفتن به آدرس:

```
http://localhost:3000
```

### ساخت نسخه Production

```bash
npm run build
npm start
```

## ساختار پروژه

```
shilat/
├── app/
│   ├── layout.tsx      # Layout اصلی
│   ├── page.tsx        # صفحه اصلی
│   └── globals.css     # استایل‌های全局
├── components/
│   ├── Navigation.tsx  # منوی ناوبری با سبد خرید
│   ├── Hero.tsx        # بخش Hero
│   ├── Categories.tsx  # دسته‌بندی محصولات
│   ├── Products.tsx    # نمایش محصولات
│   ├── Cart.tsx        # سبد خرید
│   ├── About.tsx       # بخش درباره ما
│   ├── Services.tsx    # بخش خدمات
│   ├── Contact.tsx     # بخش تماس با ما
│   └── Footer.tsx      # فوتر
├── context/
│   └── CartContext.tsx # مدیریت سبد خرید
├── package.json
└── tailwind.config.js
```

## بخش‌های وب‌سایت

1. **Hero Section**: بخش اصلی با معرفی فروشگاه
2. **Categories**: دسته‌بندی محصولات (ماهی، غذاهای دریایی، تجهیزات و...)
3. **Products**: نمایش محصولات با امکان فیلتر و افزودن به سبد خرید
4. **About**: معرفی شرکت و ویژگی‌های کلیدی
5. **Services**: خدمات ارائه شده در زمینه شیلات
6. **Contact**: فرم تماس و اطلاعات ارتباطی
7. **Shopping Cart**: سبد خرید با امکان مدیریت محصولات
8. **Footer**: اطلاعات پایانی و لینک‌ها

## سفارشی‌سازی

### تغییر رنگ‌ها

فایل `tailwind.config.js` را ویرایش کنید و رنگ‌های `primary` و `ocean` را تغییر دهید.

### تغییر محتوا

محتوا در فایل‌های کامپوننت‌ها در پوشه `components/` قابل ویرایش است.

## تکنولوژی‌های استفاده شده

- **Next.js 14**: فریمورک React
- **TypeScript**: تایپ‌های امن
- **Tailwind CSS**: استایل‌دهی
- **Framer Motion**: انیمیشن‌ها
- **Vazirmatn Font**: فونت فارسی

## نکات مهم

- این یک دمو است و فرم تماس و درگاه پرداخت فعلاً فقط نمایشی است
- برای استفاده در Production، باید:
  - فرم تماس را به یک سرویس واقعی متصل کنید
  - درگاه پرداخت را پیاده‌سازی کنید
  - سیستم مدیریت محصولات را اضافه کنید
  - تصاویر واقعی را جایگزین ایموجی‌ها کنید
- سبد خرید با Context API مدیریت می‌شود و در حافظه مرورگر ذخیره نمی‌شود

## لایسنس

این پروژه برای استفاده در نمایشگاه شیلات طراحی شده است.

